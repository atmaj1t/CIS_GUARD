{
  "name": "cis",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-win" : "electron-packager . Miniproject --platform=win32 --arch=x64 --out=dist --overwrite",
    "package-linux" : "electron-packager . Miniproject --platform=linux --arch=x64 --out=dist --overwrite"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csv-parser": "^3.0.0",
    "csv-writer": "^1.6.0",
    "electron": "^32.0.2",
    "fs": "^0.0.1-security"
  },
  "devDependencies": {
    "electron-packager": "^17.1.2"
  }
}
